{
  "name": "open-folder-from-context-menu",
  "displayName": "Open Folder",
  "description": "Provides a context menu option on folders to quickly open the folder.",
  "version": "0.0.1",
  "publisher": "joshua-woolf",
  "repository": {
    "type": "git",
    "url": "https://github.com/joshua-woolf/open-folder-from-context-menu"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "open-folder-from-context-menu.openFolder",
        "title": "Open Folder"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "explorerResourceIsFolder",
          "command": "open-folder-from-context-menu.openFolder",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@stylistic/eslint-plugin": "^4.0.1",
    "@types/chai": "^5.0.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.13.5",
    "@types/vscode": "^1.75.0",
    "@vscode/test-electron": "^2.4.1",
    "chai": "^5.2.0",
    "eslint": "^9.21.0",
    "eslint-plugin-import": "^2.31.0",
    "mocha": "^11.1.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.1"
  }
}
